# Always revalidate the HTML so new deploys are picked up without a hard refresh.
/
  Cache-Control: public, max-age=0, must-revalidate
/index.html
  Cache-Control: public, max-age=0, must-revalidate
